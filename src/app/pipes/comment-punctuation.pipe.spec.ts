import { CommentPunctuationPipe } from './comment-punctuation.pipe';

describe('CommentPunctuationPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentPunctuationPipe();
    expect(pipe).toBeTruthy();
  });
});
